# Nginx configuration

Placeholder for Phase 5 (Production). Will contain reverse-proxy configs for:

- `app.domain.com` → Mini App
- `api.domain.com` → Backend API
- `admin.domain.com` → Admin Panel

See `NOVA_ORG_AGENT_PLAN.md` sections 17-19 (AGENT 10 — Deployment + Security).
