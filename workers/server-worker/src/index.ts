/**
 * NOVA ORG server-side compute worker (`nova-worker`).
 *
 * Phase 1 placeholder — this service will pull jobs from the queue,
 * execute compute work, and submit results for validation. Intended to
 * run on a dedicated compute machine, separate from the main bot/API VPS.
 * See NOVA_ORG_AGENT_PLAN.md sections 2.2, 16, 17.
 */
console.log("nova-org server-worker: no-op placeholder. Implemented in Phase 3.");
